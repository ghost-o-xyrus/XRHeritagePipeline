/*
 * Status bar is display-only for now — no interactions to wire.
 * This stub exists to preserve architectural symmetry; future
 * status-bar actions (cancel, retry, etc.) would be wired here.
 */

export function initStatusBar() {
    // intentionally empty for MVP
}